import graph

class Traveller:
    def __init__(self, city, current_node, target_node):
        self.city = city
        self.city__current_node_index, self.current_node = self.city.get_node_from_pos(current_node)
        self.city__target_node_index, self.target_node = self.city.get_node_from_pos(target_node)

    def get_possible_moves(self, ):

    def decide_move():

    def make_move():
